services:
  next:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: next
    ports:
      - "3000:3000"
    restart: always
    networks:
      - washingbay_network

networks:
  washingbay_network:
    external: true
